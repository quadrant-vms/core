apiVersion: v1
kind: Service
metadata:
  name: operator-ui
  namespace: quadrant-vms
  labels:
    app: operator-ui
spec:
  type: ClusterIP
  ports:
  - port: 8090
    name: http
  - port: 9091
    name: metrics
  selector:
    app: operator-ui
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator-ui
  namespace: quadrant-vms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: operator-ui
  template:
    metadata:
      labels:
        app: operator-ui
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9091"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: operator-ui
        image: quadrant-vms/operator-ui:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8090
          name: http
        - containerPort: 9091
          name: metrics
        env:
        - name: OPERATOR_UI_ADDR
          value: "0.0.0.0:8090"
        - name: AUTH_SERVICE_URL
          value: "http://auth-service:8087"
        - name: DEVICE_MANAGER_URL
          value: "http://device-manager:8088"
        - name: ADMIN_GATEWAY_URL
          value: "http://admin-gateway:8081"
        - name: RECORDER_NODE_URL
          value: "http://recorder-node:8085"
        - name: AI_SERVICE_URL
          value: "http://ai-service:8084"
        - name: ALERT_SERVICE_URL
          value: "http://alert-service:8089"
        - name: PLAYBACK_SERVICE_URL
          value: "http://playback-service:8086"
        - name: COORDINATOR_URL
          value: "http://coordinator:8082"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8090
          initialDelaySeconds: 5
          periodSeconds: 5
